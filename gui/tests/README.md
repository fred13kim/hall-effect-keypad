# Tests

Files used to test integration

- `adc_test_profile.py` tests ADC-to-output profile mapping behavior.
- `adc_test_values.py` reads and tracks Hall-sensor ADC ranges including max and min.
- `test_hid_read.py` prints out ADC value directly into terminal to verify if connection works
# Demos

- `raw_adc_demo.py` plots the raw ADC values of each of the 4 keys in real time, visualizing the analog signals.
- `demo_reaction.py` creates a reaction time test aiming to demonstrate the speed increase compared to a regular keyboard.
- `typing_demo.py` enables the user to type onto a notepad, demonstrating the customizability of expressive input.